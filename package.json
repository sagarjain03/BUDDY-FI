{
  "name": "buddy-fi",
  "version": "1.0.0",
  "private": true,
  "description": "BUDDY-FI monorepo root. Application code lives in client/ and server/.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "start:server": "npm --prefix server start",
    "start:client": "npm --prefix client run dev",
    "build:client": "npm --prefix client run build",
    "lint:client": "npm --prefix client run lint",
    "test": "npm run test:server && npm run test:e2e",
    "ci": "npm run lint:client && npm run build:client && npm run test",
    "test:server": "npm --prefix server run test",
    "test:e2e": "npm --prefix client run test"
  },
  "license": "ISC"
}
